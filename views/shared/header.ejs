<!DOCTYPE html>
<html lang="en">
<head>
    <title>App</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/font-awesome/css/font-awesome.min.css"> 
    <script src="/jquery/jquery.min.js"></script>
    <script src="/bootstrap/js/bootstrap.min.js"></script>
    <style>
 
  .error,
  .errors-list {
    font-size: 14px;
    color: #C00;
  }

  .errors-list {
    margin: 0 0 1.5em;
    padding: 0 0 0 15px;
    list-style-type: none;
  }

  .form-field-invalid .form-control {
    border-color: #C00;
  }

</style>

</head> 
<body>
 
  <div class="container">
    <br>
    <% if(error_msg && error_msg.length > 0) { %>
       <div class="alert alert-danger"><p><%= error_msg %></p></div>
     <% } %>

      <% if(error && error.length > 0) { %>
       <div class="alert alert-danger"><p><%= error %></p></div>
     <% } %>
    <!-- Passport will use the error to give message -->
     <% if(success_msg && success_msg.length > 0) { %>
       <div class="alert alert-success"><p><%= success_msg %></p></div>
     <% } %>

     <% if(info_msg && info_msg.length > 0) { %>
       <div class="alert alert-info"><p><%= info_msg %></p></div>
     <% } %>
  