<% include ../shared/header.ejs %>

<table class="table table-responsive table-hover">
                <thead>
                  <tr>
                    <th> Subject </th>
                    <th> Teacher </th>
                  </tr>
                </thead>
                <tbody>
                  <% for(i=assignments.length - 1; i >= 0; i--) { %>
                    <tr>
                      <td><%= assignments[i].subject %></td>
                      <td><%= assignments[i].teacher %></td>
                        <% } %> 
                  </tbody>
                </table>
    <% include ../shared/footer.ejs %>
